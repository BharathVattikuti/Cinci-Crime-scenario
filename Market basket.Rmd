---
title: "Customer Engagement"
author: "Bharath Vattikuti"
date: "`r.Sys.Date()"
output: html_document
---

## Packages
```{r load_packages, message = FALSE, warning = FALSE, error = FALSE}
#Load Required Packages

library(data.table) #For fast reading of csv files
library(tidyverse) # For data manipulation
```

## Import Data
```{r message = FALSE, warning = FALSE }
casual <- fread(input = "Carbo Loading/Carbo-Loading CSV/causal_lookup.csv")
transactions <- fread(input = "Carbo Loading/Carbo-Loading CSV/transactions.csv")
product <- fread(input = "Carbo Loading/Carbo-Loading CSV/product_lookup.csv")
store <- fread(input = "Carbo Loading/Carbo-Loading CSV/store_lookup.csv")
```

## checking each dataset
#transactions data

str(transactions)
head(transactions)

